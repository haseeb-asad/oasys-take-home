# Runtime and dev dependencies for the pip + .venv setup (setup.sh installs these).
# Floors set to the latest releases verified on Python 3.14.
fastapi>=0.138
uvicorn[standard]>=0.49
pydantic>=2.13
pydantic-settings>=2.14
email-validator>=2.2
sqlalchemy>=2.0.51
alembic>=1.18
psycopg[binary]>=3.3
bcrypt>=5
pyjwt>=2.13
python-multipart>=0.0.32
# greenlet is a SQLAlchemy x86_64-only transitive dep (unused by our sync stack,
# not installed on arm64); 3.5.3 shipped no installable wheel, so exclude it.
greenlet!=3.5.3; platform_machine == "x86_64"

# Dev / test
ruff>=0.15
mypy>=2.1
pytest>=9.1
httpx>=0.28
