services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: kinetic
      POSTGRES_PASSWORD: kinetic
      POSTGRES_DB: kinetic
    ports:
      - "5432:5432"
    volumes:
      - kinetic_pgdata:/var/lib/postgresql/data

volumes:
  kinetic_pgdata:
